# 2026.02.12
# при очередной попытке пред. вариант не заработал, пришлось найти новый


cmake_minimum_required(VERSION 3.10)

set( CMAKE_VERBOSE_MAKEFILE on )


project(FltkCMakeExample CXX)


# set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11 -Wall")
# set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall")



set(FLTK ${CMAKE_SOURCE_DIR}/../fltk-1.4.4/)

set(FLTK_DIR ${FLTK}build/ CACHE STRING 
    "FLTK binary folder with FLTKConfig.cmake")


find_package(FLTK REQUIRED CONFIG)


# include_directories(${FLTK})
  
set(SOURCES src/main.cpp)
 
# add the executable
add_executable(${PROJECT_NAME} ${SOURCES})

target_link_libraries(${PROJECT_NAME} PRIVATE fltk::fltk)

