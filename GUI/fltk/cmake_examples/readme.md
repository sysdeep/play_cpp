# FLTK CMake examples

- https://github.com/Albrecht-S/fltk-build/tree/master



Custom fltk build
```bash
git clone https://github.com
cd fltk
mkdir build
cd build
cmake .. -DCMAKE_INSTALL_PREFIX=/path/to/custom/fltk_install
cmake --build . --target install
```
