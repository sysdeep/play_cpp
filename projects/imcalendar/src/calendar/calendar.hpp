#pragma once
#include <iostream>
#include <stdexcept>
#include <array>
#include <string>
#include "core.hpp"

namespace calendar
{
    // ---------------------------------------------------------------
    // 1. Проверка високосного года
    // ---------------------------------------------------------------
    constexpr bool is_leap_year(int year) noexcept
    {
        // Правила григорианского календаря:
        //   - делится на 4 → потенциально високосный,
        //   - но если делится на 100 → не високосный,
        //   - если при этом делится на 400 → всё‑таки високосный.
        return (year % 4 == 0) && ((year % 100 != 0) || (year % 400 == 0));
    }

    // ---------------------------------------------------------------
    // 2. Таблица дней в обычных месяцах (без учёта февраля)
    // ---------------------------------------------------------------
    constexpr std::array<int, 12> days_in_common_month{
        31, // Январь
        28, // Февраль (будет скорректирован ниже)
        31, // Март
        30, // Апрель
        31, // Май
        30, // Июнь
        31, // Июль
        31, // Август
        30, // Сентябрь
        31, // Октябрь
        30, // Ноябрь
        31  // Декабрь
    };

    // ---------------------------------------------------------------
    // 3. Основная функция
    // ---------------------------------------------------------------
    int days_in_month(int year, int month);
    // {
    //     if (month < 1 || month > 12)
    //         throw std::out_of_range("Month must be in the range 1..12");

    //     // Февраль – отдельный случай, потому что количество дней зависит от года
    //     if (month == 2)
    //         return is_leap_year(year) ? 29 : 28;

    //     // Для остальных месяцев берём значение из готовой таблицы
    //     return days_in_common_month[month - 1];
    // }

    // ---------------------------------------------------------------

    // ---------------------------------------------------------------
    // 2. Строковое представление (для вывода)
    // ---------------------------------------------------------------

    // std::string to_string(core::Weekday d) noexcept;

    // ---------------------------------------------------------------
    // 3. Проверка корректности входных параметров
    // ---------------------------------------------------------------
    // void validate_date(int day, int month, int year);

    // ---------------------------------------------------------------
    // 4. Алгоритм Зеллера (Gregorian)
    // ---------------------------------------------------------------
    Weekday day_of_week(int day, int month, int year) noexcept;

    // ---------------------------------------------------------------
    // 5. Обёртка, которая проверяет входные данные (не constexpr)
    // ---------------------------------------------------------------
    // Weekday day_of_week_checked(int day, int month, int year);
}